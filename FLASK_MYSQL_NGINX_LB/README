flask_mysql_nginx_lb/
│
├── docker-compose.yml           # Orchestrates all services: MySQL, Flask apps, NGINX
│
├── nginx/
│   └── nginx.conf               # NGINX load balancer configuration
│
├── db/
│   └── init.sql                 # MySQL initialization script (database & table creation)
│
├── app1/
│   ├── app.py                   # Flask App 1 code
│   ├── requirements.txt         # Python dependencies (flask, mysql-connector-python)
│   └── Dockerfile               # Dockerfile for Flask App 1
│
└── app2/
    ├── app.py                   # Flask App 2 code (same as App 1 with app_name="App 2")
    ├── requirements.txt         # Python dependencies (same as App 1)
    └── Dockerfile               # Dockerfile for Flask App 2

          ┌──────────────────────────────┐
          │       NGINX Load Balancer    │
          │       Port 8080 on Host      │
          └──────────────┬───────────────┘
                         │
          ┌──────────────┴───────────────┐
          │                              │
    ┌──────────────┐                ┌──────────────┐
    │  Flask App 1 │                │  Flask App 2 │
    │  (Port 5000) │                │  (Port 5000) │
    └──────────────┘                └──────────────┘
                         │
                   ┌──────────────┐
                   │   MySQL DB   │
                   └──────────────┘
